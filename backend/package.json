{
  "name": "backend",
  "version": "1.0.0",
  "type": "module",
  "packageManager": "bun",
  "scripts": {
    "build": "cd ../frontend && bun install && vite build --outDir=../backend/frontend/dist"
  },
  "dependencies": {
    "bcrypt": "^5.1.1",
    "encore.dev": "^1.48.13"
  },
  "devDependencies": {
    "typescript": "^5.8.3"
  }
}